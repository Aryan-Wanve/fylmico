{
  "name": "fylmico",
  "version": "0.1.0",
  "private": true,
  "description": "Fylmico - The Operating System for Creative Production.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22.13.0",
    "npm": ">=10.9.0"
  },
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm --workspace @fylmico/web run dev",
    "build": "npm --workspace @fylmico/web run build",
    "start": "npm --workspace @fylmico/web run start",
    "lint": "npm --workspace @fylmico/web run lint",
    "typecheck": "npm --workspace @fylmico/web run typecheck",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "verify": "npm run lint && npm run typecheck && npm run format:check && npm run build",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,mjs,cjs,json,md,css,yml,yaml}": [
      "prettier --write"
    ],
    "apps/web/**/*.{js,jsx,ts,tsx}": [
      "eslint --max-warnings=0"
    ]
  },
  "devDependencies": {
    "eslint": "9.39.4",
    "eslint-config-prettier": "10.1.8",
    "husky": "9.1.7",
    "lint-staged": "16.4.0",
    "prettier": "3.9.1",
    "prettier-plugin-tailwindcss": "0.8.0"
  }
}
